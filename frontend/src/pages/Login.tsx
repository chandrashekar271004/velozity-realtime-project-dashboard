import {FormEvent,useState} from 'react';import {useAuth} from '../context/AuthContext';import {useNavigate} from 'react-router-dom';export default function Login(){const[e,setE]=useState('admin@velozity.local');const[p,setP]=useState('Password@123');const[err,setErr]=useState('');const{login}=useAuth();const nav=useNavigate();const submit=async(x:FormEvent)=>{x.preventDefault();try{await login(e,p);nav('/')}catch{setErr('Invalid credentials')}};return <div className="login"><form onSubmit={submit}><h1>Velozity Dashboard</h1><p>Real-time client project management</p>{err&&<div className="error">{err}</div>}<input value={e} onChange={x=>setE(x.target.value)} placeholder="Email"/><input type="password" value={p} onChange={x=>setP(x.target.value)} placeholder="Password"/><button>Sign in</button><small>Seed password: Password@123</small></form></div>}
